# 高校设备预约系统 - 开发方案2（设备单元追踪）

## 一、项目背景

- 后端：RuoYi-Vue（D:\AAAA\CERS\RuoYi-Vue）
- 管理端前端：RuoYi-Vue3（D:\AAAA\CERS\RuoYi-Vue3）
- 移动端：RuoYi-App（D:\AAAA\CERS\RuoYi-App）

## 二、已完成模块

1. 设备分类管理（eq_category）- 树形分类，支持两级
2. 设备信息管理（eq_equipment）- 包含库存管理
3. 预约订单基础 CRUD（res_order）- 仅增删改查，无业务逻辑

## 三、核心需求（方案2）

### 设备分类
- **追踪单台设备（贵重）**：示波器、电脑等，需要分配唯一编号
- **不追踪单台设备（普通）**：电阻包、篮球等，只管理总库存

### 编号格式
- 格式：`设备名称-序号`
- 示例：`示波器-001`、`示波器-002`
- 序号固定3位数字，不足前补0

### 业务流程
1. 新增设备时勾选"追踪单台"→ 自动生成对应数量的设备单元编号
2. 用户预约时选择设备 → 系统自动分配可用编号
3. 管理员审批时直接看到分配的编号 → 无需手动核对

## 四、数据库设计

### 4.1 新增数据表

```sql
-- 设备单元表
CREATE TABLE `eq_equipment_unit` (
  `unit_id` BIGINT NOT NULL AUTO_INCREMENT COMMENT '单元ID',
  `equipment_id` BIGINT NOT NULL COMMENT '所属设备ID',
  `unit_code` VARCHAR(50) NOT NULL COMMENT '设备编号（如：示波器-001）',
  `status` CHAR(1) DEFAULT '0' COMMENT '状态 (0=在库 1=借出 2=维修 3=报废)',
  `borrower_id` BIGINT DEFAULT NULL COMMENT '当前借用人ID',
  `borrow_order_id` BIGINT DEFAULT NULL COMMENT '当前借用订单ID',
  `borrow_time` DATETIME DEFAULT NULL COMMENT '借出时间',
  `expected_return_time` DATETIME DEFAULT NULL COMMENT '预计归还时间',
  `actual_return_time` DATETIME DEFAULT NULL COMMENT '实际归还时间',
  `return_status` CHAR(1) DEFAULT '0' COMMENT '归还状态 (0=未归还 1=正常归还 2=损坏归还)',
  `damage_remark` VARCHAR(500) DEFAULT NULL COMMENT '损坏备注',
  `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
  `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`unit_id`),
  UNIQUE KEY `uk_equip_code` (`equipment_id`, `unit_code`),
  KEY `idx_equipment_id` (`equipment_id`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='设备单元表';
```

### 4.2 修改现有表

```sql
-- eq_equipment 新增字段
ALTER TABLE `eq_equipment`
  ADD COLUMN `track_unit` CHAR(1) DEFAULT '0' COMMENT '是否追踪单台设备 (0=否 1=是)',
  ADD COLUMN `unit_code_prefix` VARCHAR(30) DEFAULT NULL COMMENT '编号前缀';

-- res_order 新增字段
ALTER TABLE `res_order`
  ADD COLUMN `assigned_unit_codes` VARCHAR(500) DEFAULT NULL COMMENT '分配的设备编号列表',
  ADD COLUMN `approver_id` BIGINT DEFAULT NULL COMMENT '审批人ID',
  ADD COLUMN `approver_name` VARCHAR(64) DEFAULT NULL COMMENT '审批人姓名',
  ADD COLUMN `approve_time` DATETIME DEFAULT NULL COMMENT '审批时间',
  ADD COLUMN `reject_reason` VARCHAR(500) DEFAULT NULL COMMENT '拒绝原因',
  ADD COLUMN `return_status` CHAR(1) DEFAULT '0' COMMENT '归还状态';
```

### 4.3 新增字典数据

```sql
-- 设备单元状态字典
INSERT INTO sys_dict_type (dict_name, dict_type, status) VALUES ('设备单元状态', 'eq_unit_status', '0');
INSERT INTO sys_dict_data (dict_sort, dict_label, dict_value, dict_type) VALUES
(1, '在库', '0', 'eq_unit_status'),
(2, '借出', '1', 'eq_unit_status'),
(3, '维修', '2', 'eq_unit_status'),
(4, '报废', '3', 'eq_unit_status');

-- 归还状态字典
INSERT INTO sys_dict_type (dict_name, dict_type, status) VALUES ('归还状态', 'eq_return_status', '0');
INSERT INTO sys_dict_data (dict_sort, dict_label, dict_value, dict_type) VALUES
(1, '未归还', '0', 'eq_return_status'),
(2, '正常归还', '1', 'eq_return_status'),
(3, '损坏归还', '2', 'eq_return_status');

-- 预约单状态更新（新增状态）
UPDATE sys_dict_data SET dict_label = '待审核', dict_sort = 1 WHERE dict_type = 'res_order_status' AND dict_value = '0';
UPDATE sys_dict_data SET dict_label = '已通过', dict_sort = 2 WHERE dict_type = 'res_order_status' AND dict_value = '1';
UPDATE sys_dict_data SET dict_label = '已拒绝', dict_sort = 3 WHERE dict_type = 'res_order_status' AND dict_value = '2';
UPDATE sys_dict_data SET dict_label = '已取消', dict_sort = 4 WHERE dict_type = 'res_order_status' AND dict_value = '3';
INSERT INTO sys_dict_data (dict_sort, dict_label, dict_value, dict_type) VALUES
(5, '使用中', '4', 'res_order_status'),
(6, '已归还', '5', 'res_order_status');
```

## 五、后端代码改动

### 5.1 新增文件

| 文件 | 路径 | 说明 |
|---|---|---|
| EqEquipmentUnit.java | ruoyi-system/.../unit/domain/ | 设备单元实体类 |
| EqEquipmentUnitMapper.java | ruoyi-system/.../unit/mapper/ | Mapper接口 |
| EqEquipmentUnitMapper.xml | ruoyi-system/.../resources/mapper/equipment/unit/ | Mapper XML |
| IEqEquipmentUnitService.java | ruoyi-system/.../unit/service/ | 服务接口（可选） |
| EqEquipmentUnitServiceImpl.java | ruoyi-system/.../unit/service/impl/ | 服务实现（可选） |

### 5.2 修改文件

| 文件 | 改动说明 |
|---|---|
| EqEquipment.java | 新增 trackUnit、unitCodePrefix 字段 |
| EqEquipmentServiceImpl.java | insert 时自动生成设备单元 |
| ResOrder.java | 新增 assignedUnitCodes、approverId 等字段 |
| ResOrderServiceImpl.java | 新增 approveOrder、rejectOrder、returnOrder 方法 |
| ResOrderMapper.xml | 新增查询、更新SQL |
| ResOrderController.java | 新增 /approve、/reject、/return、/myList 接口 |

### 5.3 核心业务逻辑

1. **新增设备时**：如果 trackUnit='1'，循环生成 totalStock 数量的设备单元
2. **用户预约时**：查询可用设备单元，自动分配编号
3. **审批通过时**：更新设备单元状态为"借出"，如果是不追踪设备则扣减库存
4. **归还时**：更新设备单元状态为"在库"，恢复库存

## 六、前端代码改动

### 6.1 管理端 - 设备管理页面

文件：`ruoyi-vue3/src/views/system/equipment/index.vue`

改动：
- 新增表单字段：是否追踪单台（radio）、编号前缀（input）
- 新增表格列：是否追踪

### 6.2 管理端 - 预约订单页面

文件：`ruoyi-vue3/src/views/system/reserveOrder/index.vue`

改动：
- 新增表格列：分配的设备编号、审批人、审批时间、拒绝原因
- 新增审批对话框：通过/拒绝/归还
- 新增按钮权限：approve、reject、return

### 6.3 API

文件：`ruoyi-vue3/src/api/system/reserveOrder.js`

新增：
- approveOrder()
- rejectOrder()
- returnOrder()
- myOrderList()

## 七、开发顺序

1. 创建数据库表和字典
2. 新增 EqEquipmentUnit 实体类、Mapper、XML
3. 修改 EqEquipment 实体类
4. 修改 EqEquipmentServiceImpl（自动生成单元）
5. 修改 ResOrder 实体类
6. 修改 ResOrderMapper.xml
7. 修改 ResOrderServiceImpl（审批逻辑）
8. 修改 ResOrderController
9. 修改管理端设备管理页面
10. 修改管理端预约订单页面
11. 修改 API
12. 测试调试

## 八、参考文件路径

- 设备实体类：`D:\AAAA\CERS\RuoYi-Vue\ruoyi-system\src\main\java\com\ruoyi\equipment\info\domain\EqEquipment.java`
- 预约实体类：`D:\AAAA\CERS\RuoYi-Vue\ruoyi-system\src\main\java\com\ruoyi\equipment\order\domain\ResOrder.java`
- 预约服务实现：`D:\AAAA\CERS\RuoYi-Vue\ruoyi-system\src\main\java\com\ruoyi\equipment\order\service\impl\ResOrderServiceImpl.java`
- 预约控制器：`D:\AAAA\CERS\RuoYi-Vue\ruoyi-admin\src\main\java\com\ruoyi\equipment\order\controller\ResOrderController.java`
- 预约前端页面：`D:\AAAA\CERS\RuoYi-Vue3\src\views\system\reserveOrder\index.vue`
- 设备前端页面：`D:\AAAA\CERS\RuoYi-Vue3\src\views\system\equipment\index.vue`
